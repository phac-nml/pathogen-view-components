<%= render(Pathogen::BaseComponent.new(**@system_arguments)) do %>
  <% if leading_visual %>
    <span class="inline-flex shrink-0 items-center" aria-hidden="true" inert><%= leading_visual %></span>
  <% end %>

  <span><%= text %></span>
<% end %>
