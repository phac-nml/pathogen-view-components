<div class="flex max-w-4xl flex-col gap-8 p-4">
  <section>
    <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-2") { "Inline navigation" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      Use <%= render Pathogen::Typography::Code.new { "Pathogen::Link" } %> for underlined inline navigation inside body
      copy, help text, and table cells. Accent colour appears on hover through
      <%= render Pathogen::Typography::Code.new { "--pvc-color-accent-strong" } %>.
    <% end %>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
      <%= render Pathogen::Typography::Text.new do %>
        Specimen <strong>SPC-24018</strong> was submitted on 12 May 2026. Review the
        <%= render Pathogen::Link.new(href: "#qc-summary") { "QC summary" } %> before approving the run, or open the
        <%= render Pathogen::Link.new(href: "#run-history") { "run history" } %> for prior attempts.
      <% end %>
    </div>
  </section>

  <section>
    <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-2") { "Detail panel links" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      Group related links in compact stacks when they support the same record or task. Keep labels specific.
    <% end %>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)]">
      <div class="border-b border-[var(--pvc-color-border)] px-4 py-3">
        <%= render Pathogen::Typography::Heading.new(level: 5, class: "font-semibold") { "Run INV-8821" } %>
        <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mt-1") { "Salmonella enterica · Whole genome assembly" } %>
      </div>

      <ul class="divide-y divide-[var(--pvc-color-border)] px-4 py-2 text-sm">
        <li class="py-2">
          <%= render Pathogen::Link.new(href: "#protocol") { "View sequencing protocol" } %>
        </li>

        <li class="py-2">
          <%= render Pathogen::Link.new(href: "#metadata") { "Edit specimen metadata" } %>
        </li>

        <li class="py-2">
          <%= render Pathogen::Link.new(href: "#attachments") { "Download attachment bundle" } %>
        </li>
      </ul>
    </div>
  </section>

  <section>
    <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-2") { "External links" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      External hosts open in a new tab with
      <%= render Pathogen::Typography::Code.new { "rel=\"noopener noreferrer\"" } %> and an accessible label when the
      link text alone is not enough context.
    <% end %>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
      <%= render Pathogen::Typography::Text.new do %>
        Reference assembly
        <%= render Pathogen::Link.new(href: "https://www.ncbi.nlm.nih.gov/") { "NCBI GenBank record GCA_000006945.2" } %>
        for lineage comparison.
      <% end %>
    </div>
  </section>

  <section>
    <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-2") { "With tooltip" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      Add a tooltip when the label is short but the destination or side effect needs clarification. Tooltip content
      supplements the link; it does not replace the visible label.
    <% end %>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
      <%= render Pathogen::Typography::Text.new do %>
        Export status is pending validation. See
        <%= render Pathogen::Link.new(href: "#export-policy") do |component| %>
          <%= component.with_tooltip(text: "Exports are retained for 30 days in the project workspace.") %> export
          retention policy
        <% end %>
        for storage limits.
      <% end %>
    </div>
  </section>

  <section>
    <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-2") { "Link vs button" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      Links navigate or reveal related content. Buttons commit actions. In toolbars and form footers, prefer
      <%= render Pathogen::Typography::Code.new { "Pathogen::Button" } %>. In prose and inline help, prefer
      <%= render Pathogen::Typography::Code.new { "Pathogen::Link" } %>.
    <% end %>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
      <div class="flex flex-wrap items-center gap-x-4 gap-y-2 text-sm">
        <%= render Pathogen::Typography::Supporting.new(tag: :span, variant: :muted) { "Inline:" } %>
        <%= render Pathogen::Link.new(href: "#sample") { "Open specimen SPC-24018" } %>

        <%= render Pathogen::Typography::Supporting.new(tag: :span, variant: :muted, class: "ml-2") { "Action:" } %>
        <%= render Pathogen::Button.new(tone: :primary, emphasis: :solid, text: 'Approve run') %>
      </div>
    </div>
  </section>

  <section>
    <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-2") { "Focus and keyboard" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      Tab to a link and confirm the focus ring uses
      <%= render Pathogen::Typography::Code.new { "--pvc-color-focus" } %> in light and dark mode. Hover thickens the
      underline without removing readable contrast.
    <% end %>

    <div
      class="
        rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)]
        bg-[var(--pvc-color-surface-muted)] p-4
      "
    >
      <%= render Pathogen::Link.new(href: "#focus-example") { "Tab here to inspect focus ring" } %>
    </div>
  </section>
</div>
