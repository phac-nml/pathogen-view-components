<div class="flex max-w-4xl flex-col gap-8 p-4">
  <section>
    <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-2") { "Standalone form actions" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      <%= render Pathogen::Typography::Code.new { "pathogen_button_to" } %> mirrors Rails
      <%= render Pathogen::Typography::Code.new { "button_to" } %> but renders
      <%= render Pathogen::Typography::Code.new { "Pathogen::Button" } %> so form actions match the design system. Use
      <%= render Pathogen::Typography::Code.new { "tone" } %> +
      <%= render Pathogen::Typography::Code.new { "emphasis" } %> to style submit actions.
    <% end %>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
      <div class="flex flex-wrap items-center gap-2">
        <%= pathogen_button_to "Retry analysis", "#", method: :post, tone: :primary, emphasis: :solid %>
        <%= pathogen_button_to "Cancel", "#", method: :post, tone: :neutral, emphasis: :outline %>
        <%= pathogen_button_to "Reset", "#", method: :post, tone: :neutral, emphasis: :ghost %>
      </div>
    </div>
  </section>

  <section>
    <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-2") { "Destructive actions" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      Use <%= render Pathogen::Typography::Code.new { "danger + outline" } %> for available destructive actions, and
      reserve <%= render Pathogen::Typography::Code.new { "danger + solid" } %> for explicit confirmation steps.
    <% end %>

    <div class="flex flex-col gap-4">
      <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
        <div class="flex flex-wrap items-center gap-2">
          <%= pathogen_button_to "Delete specimen", "#", method: :delete, tone: :danger, emphasis: :outline,
                data: { turbo_confirm: "Delete this record?" } %>

          <%= pathogen_button_to "Archive", "#", method: :post, tone: :neutral, emphasis: :outline %>
        </div>
      </div>

      <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
        <%= render Pathogen::Typography::Heading.new(level: 5, class: "mb-3 font-semibold") { "Confirm delete" } %>

        <div class="flex flex-wrap items-center gap-2">
          <%= pathogen_button_to "Delete permanently", "#", method: :delete, tone: :danger, emphasis: :solid,
                data: { turbo_confirm: "This cannot be undone. Delete permanently?" } %>

          <%= pathogen_button_to "Keep record", "#", method: :post, tone: :neutral, emphasis: :outline %>
        </div>
      </div>
    </div>
  </section>
</div>
