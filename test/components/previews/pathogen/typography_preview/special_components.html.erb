<%# herb:formatter ignore %>

<div class="flex max-w-4xl flex-col gap-8 p-4">
  <section>
    <h2 class="mb-2 text-base font-semibold text-[var(--pvc-color-text)]">Protocol installation</h2>

    <p class="mb-4 text-sm text-[var(--pvc-color-text-muted)]">
      Lists, code, and section wrappers in a realistic setup flow. Section validates heading hierarchy in development.
    </p>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
      <%= render Pathogen::Typography::Section.new(level: 2) do |section| %>
        <% section.with_heading { "Prerequisites" } %>

        <%= render Pathogen::Typography::List.new(class: "mt-3") do |list| %>
          <%= list.with_item { "PostgreSQL 14 or higher" } %>
          <%= list.with_item { "Ruby 3.3 or higher" } %>
          <%= list.with_item { "Access to the reference sequence bundle" } %>
        <% end %>
      <% end %>

      <%= render Pathogen::Typography::Section.new(level: 2, class: "mt-6") do |section| %>
        <% section.with_heading { "Configure environment" } %>

        <%= render Pathogen::Typography::Text.new(class: "mt-3") do %>
          Export the protocol path before running setup. Use <%= render(Pathogen::Typography::Code.new) { "bin/setup" } %> from the application root.
        <% end %>

        <%= render Pathogen::Typography::CodeBlock.new(language: "bash", class: "mt-3") do %>
export PROTOCOL_ROOT=/data/protocols/wgs-bacterial-v3.2
bin/setup
        <% end %>
      <% end %>
    </div>
  </section>

  <section>
    <h2 class="mb-2 text-base font-semibold text-[var(--pvc-color-text)]">Card header group</h2>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
      <%= render Pathogen::Typography::HeadingGroup.new(level: 3, spacing: :compact) do |group| %>
        <%= group.with_heading { "COVID-19 Analysis" } %>
        <%= group.with_metadata { "45 samples · Running" } %>
      <% end %>
    </div>
  </section>

  <div class="rounded-md border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4 text-sm text-[var(--pvc-color-text-muted)]">
    <p class="font-semibold text-[var(--pvc-color-text)]">Usage</p>
    <ul class="mt-2 space-y-1.5 leading-relaxed">
      <li><%= render(Pathogen::Typography::Code.new) { "render Pathogen::Typography::List.new" } %> — steps</li>
      <li><%= render(Pathogen::Typography::Code.new) { "render Pathogen::Typography::Code.new" } %> and <%= render(Pathogen::Typography::Code.new) { "render Pathogen::Typography::CodeBlock.new" } %> — commands</li>
      <li><%= render(Pathogen::Typography::Code.new) { "render Pathogen::Typography::Section.new" } %> — bounded regions with a heading slot</li>
    </ul>
  </div>
</div>
