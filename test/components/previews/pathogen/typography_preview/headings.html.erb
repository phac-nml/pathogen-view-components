<%# herb:formatter ignore %>

<div class="flex max-w-4xl flex-col gap-8 p-4">
  <section>
    <h2 class="mb-2 text-base font-semibold text-[var(--pvc-color-text)]">Specimen record headings</h2>

    <p class="mb-4 text-sm text-[var(--pvc-color-text-muted)]">
      Semantic levels map to role tokens. Use sequential h1–h6 without skipping levels inside a page region.
    </p>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
      <%= render Pathogen::Typography::Heading.new(level: 1) { "SPEC-2026-00891" } %>

      <%= render Pathogen::Typography::Supporting.new(variant: :muted, size: :meta, class: "mt-1") do %>
        Collected 2026-06-09 · Provincial public health lab
      <% end %>

      <%= render Pathogen::Typography::Heading.new(level: 2, class: "mt-6") { "Collection details" } %>

      <%= render Pathogen::Typography::Text.new(class: "mt-2") do %>
        Nasopharyngeal swab received at the reference laboratory under cold-chain protocol B-12.
      <% end %>

      <%= render Pathogen::Typography::Heading.new(level: 3, class: "mt-6") { "Lab results" } %>

      <%= render Pathogen::Typography::Text.new(class: "mt-2") do %>
        Culture negative. WGS pending on paired isolate SPEC-2026-00891-A.
      <% end %>

      <%= render Pathogen::Typography::Heading.new(level: 4, class: "mt-6") { "QC flags" } %>

      <%= render Pathogen::Typography::Text.new(variant: :muted, class: "mt-2") do %>
        Coverage below threshold on contig 3 — review required before release.
      <% end %>
    </div>
  </section>

  <section>
    <h2 class="mb-2 text-base font-semibold text-[var(--pvc-color-text)]">Colour variants</h2>

    <p class="mb-4 text-sm text-[var(--pvc-color-text-muted)]">
      Default for primary labels; muted for secondary section titles; subdued and inverse for constrained surfaces.
    </p>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4 space-y-3">
      <%= render Pathogen::Typography::Heading.new(level: 3, variant: :default) { "Default section title" } %>
      <%= render Pathogen::Typography::Heading.new(level: 3, variant: :muted) { "Muted section title" } %>
      <%= render Pathogen::Typography::Heading.new(level: 3, variant: :subdued) { "Subdued section title" } %>

      <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border-strong)] bg-neutral-950 p-3">
        <%= render Pathogen::Typography::Heading.new(level: 3, variant: :inverse) { "Inverse section title" } %>
      </div>
    </div>
  </section>

  <div class="rounded-md border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4 text-sm text-[var(--pvc-color-text-muted)]">
    <p class="font-semibold text-[var(--pvc-color-text)]">Usage</p>
    <p class="mt-2">
      Use sequential heading levels inside a page region. Do not override page-title sizing with custom classes;
      hierarchy comes from level, weight, and placement.
    </p>
    <pre class="mt-2 overflow-x-auto rounded-[var(--pvc-radius-control)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface-muted)] p-3 text-xs text-[var(--pvc-color-text)]"><code>&lt;%= render Pathogen::Typography::Heading.new(level: 2) { "Section" } %&gt;</code></pre>
  </div>
</div>
