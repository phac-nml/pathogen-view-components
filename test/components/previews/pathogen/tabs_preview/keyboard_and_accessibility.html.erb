<%# herb:formatter ignore %>

<div class="flex max-w-4xl flex-col gap-8 p-4">
  <section>
    <%= render Pathogen::Typography::Heading.new(level: 2, class: "mb-2") { "Keyboard navigation" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      Tabs follow the W3C automatic activation pattern. Focus a tab, then use arrow keys to move and activate.
      <kbd class="rounded-sm border border-[var(--pvc-color-border-strong)] px-1.5 py-0.5 text-[length:var(--type-meta)]">Home</kbd> and
      <kbd class="rounded-sm border border-[var(--pvc-color-border-strong)] px-1.5 py-0.5 text-[length:var(--type-meta)]">End</kbd>
      jump to the first and last tab. Press
      <kbd class="rounded-sm border border-[var(--pvc-color-border-strong)] px-1.5 py-0.5 text-[length:var(--type-meta)]">Tab</kbd>
      to leave the tab list and enter the panel.
    <% end %>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
      <%= render(Pathogen::Tabs.new(id: 'keyboard-demo-tabs', label: 'Specimen review sections')) do |tabs| %>
        <% tabs.with_tab(id: 'kbd-tab-collection', label: 'Collection', selected: true) %>
        <% tabs.with_tab(id: 'kbd-tab-lab', label: 'Lab results') %>
        <% tabs.with_tab(id: 'kbd-tab-qc', label: 'QC flags') %>
        <% tabs.with_tab(id: 'kbd-tab-audit', label: 'Audit') %>

        <% tabs.with_panel(id: 'kbd-panel-collection', tab_id: 'kbd-tab-collection') do %>
          <dl class="grid gap-3 text-[length:var(--type-control)] sm:grid-cols-2">
            <div>
              <dt class="font-semibold text-[var(--pvc-color-text)]">Specimen ID</dt>
              <dd class="font-mono text-[var(--pvc-color-text-muted)]">SPEC-2026-00891</dd>
            </div>
            <div>
              <dt class="font-semibold text-[var(--pvc-color-text)]">Collected</dt>
              <dd class="text-[var(--pvc-color-text-muted)]">2026-06-09</dd>
            </div>
          </dl>
        <% end %>

        <% tabs.with_panel(id: 'kbd-panel-lab', tab_id: 'kbd-tab-lab') do %>
          <%= render Pathogen::Typography::Text.new(variant: :muted) do %>
            Culture negative. WGS pending on paired isolate SPEC-2026-00891-A.
          <% end %>
        <% end %>

        <% tabs.with_panel(id: 'kbd-panel-qc', tab_id: 'kbd-tab-qc') do %>
          <%= render Pathogen::Typography::Supporting.new(class: "text-[var(--pvc-color-warning)]") do %>
            Coverage below threshold on contig 3 — review required.
          <% end %>
        <% end %>

        <% tabs.with_panel(id: 'kbd-panel-audit', tab_id: 'kbd-tab-audit') do %>
          <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
            Last updated by lab-seq-review at 2026-06-10 14:32 UTC.
          <% end %>

          <%= render Pathogen::Button.new(tone: :primary, emphasis: :solid, text: 'Open audit log') %>
        <% end %>
      <% end %>
    </div>
  </section>

  <section>
    <%= render Pathogen::Typography::Heading.new(level: 2, class: "mb-2") { "ARIA wiring" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      The component sets roles, labels, and roving tabindex so screen readers announce tab lists, selection, and panels
      consistently.
    <% end %>

    <div class="grid gap-4 lg:grid-cols-2">
      <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
        <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-3") { "Tab list" } %>

        <dl class="space-y-2 text-[length:var(--type-control)]">
          <div class="flex gap-2">
            <dt class="shrink-0"><%= render Pathogen::Typography::Code.new { "role" } %></dt>
            <dd><%= render Pathogen::Typography::Supporting.new(variant: :muted, tag: :span) do %><%= render Pathogen::Typography::Code.new { "tablist" } %> on <%= render Pathogen::Typography::Code.new { "nav" } %><% end %></dd>
          </div>
          <div class="flex gap-2">
            <dt class="shrink-0"><%= render Pathogen::Typography::Code.new { "aria-label" } %></dt>
            <dd><%= render Pathogen::Typography::Supporting.new(variant: :muted, tag: :span) { "Names the tab group" } %></dd>
          </div>
          <div class="flex gap-2">
            <dt class="shrink-0"><%= render Pathogen::Typography::Code.new { "aria-orientation" } %></dt>
            <dd><%= render Pathogen::Typography::Supporting.new(variant: :muted, tag: :span) do %><%= render Pathogen::Typography::Code.new { "horizontal" } %> or <%= render Pathogen::Typography::Code.new { "vertical" } %><% end %></dd>
          </div>
        </dl>
      </div>

      <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
        <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-3") { "Tab buttons" } %>

        <dl class="space-y-2 text-[length:var(--type-control)]">
          <div class="flex gap-2">
            <dt class="shrink-0"><%= render Pathogen::Typography::Code.new { "role" } %></dt>
            <dd><%= render Pathogen::Typography::Supporting.new(variant: :muted, tag: :span) do %><%= render Pathogen::Typography::Code.new { "tab" } %> on native <%= render Pathogen::Typography::Code.new { "button" } %><% end %></dd>
          </div>
          <div class="flex gap-2">
            <dt class="shrink-0"><%= render Pathogen::Typography::Code.new { "aria-selected" } %></dt>
            <dd><%= render Pathogen::Typography::Supporting.new(variant: :muted, tag: :span) do %><%= render Pathogen::Typography::Code.new { "true" } %> on the active tab only<% end %></dd>
          </div>
          <div class="flex gap-2">
            <dt class="shrink-0"><%= render Pathogen::Typography::Code.new { "tabindex" } %></dt>
            <dd><%= render Pathogen::Typography::Supporting.new(variant: :muted, tag: :span) do %>Roving: <%= render Pathogen::Typography::Code.new { "0" } %> active, <%= render Pathogen::Typography::Code.new { "-1" } %> inactive<% end %></dd>
          </div>
        </dl>
      </div>

      <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4 lg:col-span-2">
        <%= render Pathogen::Typography::Heading.new(level: 3, class: "mb-3") { "Panels" } %>

        <dl class="grid gap-2 text-[length:var(--type-control)] sm:grid-cols-2">
          <div class="flex gap-2">
            <dt class="shrink-0"><%= render Pathogen::Typography::Code.new { "role" } %></dt>
            <dd><%= render Pathogen::Typography::Supporting.new(variant: :muted, tag: :span) do %><%= render Pathogen::Typography::Code.new { "tabpanel" } %><% end %></dd>
          </div>
          <div class="flex gap-2">
            <dt class="shrink-0"><%= render Pathogen::Typography::Code.new { "aria-labelledby" } %></dt>
            <dd><%= render Pathogen::Typography::Supporting.new(variant: :muted, tag: :span) { "References the controlling tab id" } %></dd>
          </div>
          <div class="flex gap-2">
            <dt class="shrink-0"><%= render Pathogen::Typography::Code.new { "hidden" } %></dt>
            <dd><%= render Pathogen::Typography::Supporting.new(variant: :muted, tag: :span) { "Native attribute toggled for inactive panels" } %></dd>
          </div>
          <div class="flex gap-2">
            <dt class="shrink-0"><%= render Pathogen::Typography::Code.new { "data-state" } %></dt>
            <dd><%= render Pathogen::Typography::Supporting.new(variant: :muted, tag: :span) do %><%= render Pathogen::Typography::Code.new { "active" } %> / <%= render Pathogen::Typography::Code.new { "inactive" } %> for styling hooks<% end %></dd>
          </div>
        </dl>
      </div>
    </div>
  </section>
</div>
