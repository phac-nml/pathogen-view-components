<%# herb:formatter ignore %>

<div class="flex max-w-4xl flex-col gap-8 p-4">
  <section>
    <%= render Pathogen::Typography::Heading.new(level: 2, class: "mb-2") { "Bookmarkable tabs" } %>

    <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mb-4") do %>
      Pass <%= render Pathogen::Typography::Code.new { "sync_url: true" } %> to mirror the active tab in the URL hash. Reloading or sharing
      the link returns to the same section. Arrow-key navigation updates the hash as well.
    <% end %>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface)] p-4">
      <%= render(Pathogen::Tabs.new(
        id: 'url-sync-demo',
        label: 'Outbreak investigation sections',
        sync_url: true
      )) do |tabs| %>
        <% tabs.with_tab(id: 'tab-overview', label: 'Overview', selected: true) %>
        <% tabs.with_tab(id: 'tab-specimens', label: 'Specimens') %>
        <% tabs.with_tab(id: 'tab-lineage', label: 'Lineage') %>
        <% tabs.with_tab(id: 'tab-notes', label: 'Notes') %>

        <% tabs.with_panel(id: 'panel-overview', tab_id: 'tab-overview') do %>
          <dl class="grid gap-3 text-[length:var(--type-control)] sm:grid-cols-2">
            <div>
              <dt class="font-semibold text-[var(--pvc-color-text)]">Investigation</dt>
              <dd class="font-mono text-[var(--pvc-color-text-muted)]">INV-2026-003</dd>
            </div>
            <div>
              <dt class="font-semibold text-[var(--pvc-color-text)]">Status</dt>
              <dd class="text-[var(--pvc-color-warning)]">Active monitoring</dd>
            </div>
          </dl>
          <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mt-3") do %>
            The URL hash should read <%= render Pathogen::Typography::Code.new { "#tab-overview" } %> while this tab is selected.
          <% end %>
        <% end %>

        <% tabs.with_panel(id: 'panel-specimens', tab_id: 'tab-specimens') do %>
          <%= render Pathogen::Typography::Text.new(variant: :muted) do %>
            12 linked specimens across 4 jurisdictions. Last specimen added 2026-06-08.
          <% end %>
        <% end %>

        <% tabs.with_panel(id: 'panel-lineage', tab_id: 'tab-lineage') do %>
          <%= render Pathogen::Typography::Text.new(variant: :muted) do %>
            Consensus lineage: <%= render Pathogen::Typography::Code.new { "Salmonella" } %> ST-11 cluster within 3 SNPs.
          <% end %>
        <% end %>

        <% tabs.with_panel(id: 'panel-notes', tab_id: 'tab-notes') do %>
          <%= render Pathogen::Typography::Text.new(variant: :muted) do %>
            Epidemiology follow-up scheduled for 2026-06-12. Contact provincial lead before release.
          <% end %>
        <% end %>
      <% end %>
    </div>
  </section>

  <section>
    <%= render Pathogen::Typography::Heading.new(level: 2, class: "mb-2") { "Hash formats" } %>

    <div class="rounded-[var(--pvc-radius-panel)] border border-[var(--pvc-color-border)] bg-[var(--pvc-color-surface-muted)] p-4">
      <%= render Pathogen::Typography::List.new(variant: :muted) do |list| %>
        <% list.with_item do %>
          <%= render Pathogen::Typography::Code.new { "#tab-specimens" } %> — matches a tab id
        <% end %>
        <% list.with_item do %>
          <%= render Pathogen::Typography::Code.new { "#panel-lineage" } %> — matches a panel id
        <% end %>
        <% list.with_item do %>
          <%= render Pathogen::Typography::Code.new { "#tab-2" } %> — zero-based tab index fallback
        <% end %>
      <% end %>

      <%= render Pathogen::Typography::Supporting.new(variant: :muted, class: "mt-4") do %>
        Invalid hashes fall back to <%= render Pathogen::Typography::Code.new { "default_index" } %>. Hash updates use
        <%= render Pathogen::Typography::Code.new { "history.replaceState()" } %> so routine tab changes do not pollute browser history.
      <% end %>
    </div>
  </section>
</div>
